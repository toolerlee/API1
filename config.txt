# 模式設定
mode=0  # 0=一般模式, 1=重試模式

# 並發處理設定
max_concurrent_accounts=5  # 同時處理的帳號數量
thread_start_delay=0.5  # 線程啟動延遲(秒)

# 登入重試設定
max_login_attempts=5  # 登入重試次數

# 請求間隔設定
request_delay=1.0  # 每次請求之間的延遲時間(秒)
max_request_retries=3  # 請求失敗時的最大重試次數
retry_delay=5.0  # 重試之間的延遲時間(秒)

# 日期設定
start_date=2025/01/01  # 開始日期
end_date=2025/12/31  # 結束日期

# Dropbox Settings
dropbox_app_key=vjxxknfipwukyzs
dropbox_app_secret=905vcme1i68i3ke
dropbox_refresh_token=eS5fH8rlRL4AAAAAAAAAAWRwXgPu2zBfWhw4156uqtt-usI5fk8mXD-I9kPDljua
dropbox_token= # 可以留空，程式會自動 refresh
dropbox_folder=/output  # 報表上傳到 Dropbox 的哪個資料夾 (相對於 App Folder)
dropbox_account_file_path = /account/account.txt # 存放帳號密碼的 txt 檔在 Dropbox 中的完整路徑 (相對於 App Folder)

# --- API Security ---
API_ACTION_PASSWORD = 0857

# --- Flask Server Settings (Optional, Render.com 通常會用 PORT 環境變數) ---
# FLASK_HOST = 0.0.0.0
# FLASK_PORT = 5000